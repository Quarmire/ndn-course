# Pinned so every student compiles with the same compiler the witnesses were
# written and graded against. Bump deliberately alongside a course release.
[toolchain]
channel = "1.96.0"
components = ["clippy", "rustfmt"]
